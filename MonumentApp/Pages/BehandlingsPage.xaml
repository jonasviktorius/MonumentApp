<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MonumentApp.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ViewModel="using:MonumentApp.ViewModel"
    x:Class="MonumentApp.Pages.BehandlingsPage"
    mc:Ignorable="d">

    <Page.Background>

        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="Black" Offset="0"/>
            <GradientStop Color="#FFFB4848" Offset="1"/>
        </LinearGradientBrush>
    </Page.Background>

    <Grid>

        <Grid.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FFC0D4BB"/>
                <GradientStop Color="#FFDAE2D0" Offset="1"/>
                <GradientStop Color="#FFD0DAC6" Offset="0.518"/>
            </LinearGradientBrush>
        </Grid.Background>
        <ScrollViewer Padding="{x:Bind Frame.Padding, Mode=OneWay}" HorizontalAlignment="Left" Width="1280">

            <ScrollViewer.DataContext>
                <ViewModel:MonumentViewModel/>
            </ScrollViewer.DataContext>

            <ListView x:Name="listView" Height="540" Width="572" ItemsSource="{Binding MonumentSingleton.Monumenter, Mode=TwoWay}" RenderTransformOrigin="0.491,0.472"/>
        
        </ScrollViewer>
    </Grid>

</Page>
